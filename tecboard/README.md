# Anotações

Alguns elementos são por padrão em "blocos", exibidos uma margem em cima e em baixo (por default, e da pra controlar altura e largura);

Alguns elementos são "inline", exibidos em linha (não da para manipular altura e largura)
